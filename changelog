Hot fix per risolvere un problema con le API di RoutineHub che bloccava l'esecuzione corretta dello shortcut

Hot fix for RoutineHub API being too slow to respond to auto-update query, essentially stopping the shortcut from running smoothly 